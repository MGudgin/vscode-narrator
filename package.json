{
  "name": "code-narration",
  "displayName": "Code Narration",
  "description": "LLM-powered narration of code and diffs in a side pane.",
  "version": "0.1.1",
  "publisher": "gudge",
  "license": "MIT",
  "icon": "icon.png",
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "codeNarration.open",
        "title": "Open Narration",
        "category": "Code Narration",
        "icon": "$(book)"
      },
      {
        "command": "codeNarration.openDiff",
        "title": "Open Diff Narration",
        "category": "Code Narration",
        "icon": "$(git-compare)"
      },
      {
        "command": "codeNarration.refresh",
        "title": "Refresh Narration",
        "category": "Code Narration",
        "icon": "$(refresh)"
      },
      {
        "command": "codeNarration.reveal",
        "title": "Reveal Code Location",
        "category": "Code Narration"
      },
      {
        "command": "codeNarration.setApiKey",
        "title": "Set Anthropic API Key",
        "category": "Code Narration"
      },
      {
        "command": "codeNarration.pickModel",
        "title": "Pick Model",
        "category": "Code Narration"
      },
      {
        "command": "codeNarration.clearCache",
        "title": "Clear Cache",
        "category": "Code Narration"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "codeNarration.open",
          "group": "navigation@1",
          "when": "activeWebviewPanelId != 'codeNarration'"
        },
        {
          "command": "codeNarration.openDiff",
          "group": "navigation@2",
          "when": "activeWebviewPanelId != 'codeNarration'"
        }
      ],
      "commandPalette": [
        {
          "command": "codeNarration.reveal",
          "when": "false"
        }
      ]
    },
    "configuration": {
      "title": "Code Narration",
      "properties": {
        "codeNarration.provider": {
          "type": "string",
          "enum": ["vscodeLm", "anthropic"],
          "default": "vscodeLm",
          "description": "Which language model provider to use. 'vscodeLm' uses the built-in VS Code Language Model API (requires GitHub Copilot). 'anthropic' calls the Anthropic API directly using a key stored in SecretStorage."
        },
        "codeNarration.vscodeLm.modelFamily": {
          "type": "string",
          "default": "",
          "description": "Optional model family for the VS Code LM provider (e.g. 'gpt-4o', 'claude-sonnet-4'). Empty picks the first available model."
        },
        "codeNarration.anthropic.model": {
          "type": "string",
          "default": "claude-sonnet-4-6",
          "description": "Anthropic model ID. Examples: claude-sonnet-4-6, claude-opus-4-7, claude-haiku-4-5-20251001."
        },
        "codeNarration.diffBase": {
          "type": "string",
          "default": "HEAD",
          "description": "Git ref to diff against in diff narration mode."
        },
        "codeNarration.narrateOnSave": {
          "type": "boolean",
          "default": true,
          "description": "Re-narrate the file when it is saved, if the narration pane is open and showing that file."
        },
        "codeNarration.symbolConcurrency": {
          "type": "integer",
          "default": 4,
          "minimum": 1,
          "maximum": 16,
          "description": "Maximum number of language model calls to run in parallel when narrating a symbol-aware file."
        },
        "codeNarration.recurseSymbols": {
          "type": "string",
          "enum": ["auto", "always", "never"],
          "enumDescriptions": [
            "Recurse into child symbols for container-heavy languages (C#, Java, C/C++, Kotlin, Swift, Scala, F#, VB, Objective-C/C++); stay top-level only for others.",
            "Always narrate each child symbol (e.g., methods inside a class) as its own section.",
            "Never recurse; narrate only top-level symbols."
          ],
          "default": "auto",
          "scope": "language-overridable",
          "description": "Whether to narrate child symbols as their own sections. Legacy boolean values (true/false) are still honored as 'always'/'never'."
        }
      }
    }
  },
  "scripts": {
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "compile:prod": "npm run check-types && npm run lint && node esbuild.js --production",
    "watch": "node esbuild.js --watch",
    "check-types": "tsc --noEmit -p ./",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "test": "vitest run",
    "test:watch": "vitest",
    "vscode:prepublish": "npm run compile:prod",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.1.2",
    "@types/node": "^20.14.0",
    "@types/vscode": "^1.95.0",
    "@vscode/vsce": "^3.0.0",
    "esbuild": "^0.25.0",
    "eslint": "^9.10.0",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.5.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "markdown-it": "^14.1.0"
  }
}
