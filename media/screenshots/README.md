# Screenshots

Assets referenced from the top-level `README.md`.

## `narration-pane.png`

Currently a **1×1 transparent placeholder**. Replace with a real capture
showing:

- The editor on the left with a moderate file open (e.g. one of the
  extension's own `src/*.ts` files).
- The narration pane on the right, with at least one completed section
  (green status dot) and ideally one section still streaming (amber dot).
- Either light or dark theme — dark renders better against the marketplace
  background.

Keep the file size reasonable (<300 KB for a PNG). A short loop as an
APNG or animated GIF is also fine; if you swap formats, update the
`README.md` reference.

Tracked by [#37](https://github.com/MGudgin/vscode-narrator/issues/37).
